{
  "name": "example",
  "version": "1.0.0",
  "description": "Example application for abyssalJS framework",
  "main": "dist/index.js",
  "author": "Remy Rivals <rivals.remy@gmail.com>",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@types/express": "^4.11.1",
    "typescript": "^2.7.2",
    "@abyssaljs/core": "^0.1.1",
    "@abyssaljs/plugin-graphql": "^0.1.2"
  },
  "scripts": {
    "compile": "npm run clean && tsc -p .",
    "watch": "tsc -w -p .",
    "clean": "rm -rf dist",
    "start": "tsc --watch | nodemon ./dist/index.js --watch dist"
  },
  "devDependencies": {
    "nodemon": "^1.17.2"
  }
}
