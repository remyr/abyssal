{
  "name": "example",
  "version": "1.1.1",
  "description": "Example application for abyssalJS framework",
  "main": "dist/index.js",
  "author": "Remy Rivals <rivals.remy@gmail.com>",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@abyssaljs/core": "^0.2.0",
    "@abyssaljs/plugin-graphql": "^0.4.0",
    "@types/express": "^4.11.1",
    "typescript": "^2.7.2"
  },
  "scripts": {
    "compile": "npm run clean && tsc -p .",
    "watch": "tsc -w -p .",
    "clean": "rm -rf dist",
    "start": "tsc --watch | nodemon ./dist/index.js --watch dist"
  },
  "devDependencies": {
    "nodemon": "^1.17.2"
  }
}
